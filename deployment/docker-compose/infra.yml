name: 'spring-boot-microservices-course'
services:
  catalog-db:
    image: postgres:17-alpine
    container_name: catalog-dbb
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=postgres
    ports:
      - "15432:5432"
    deploy:
      resources:
        limits:
          memory: 500m
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5
